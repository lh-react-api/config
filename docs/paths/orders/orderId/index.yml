get:
  tags:
    - orders
  summary: 注文情報詳細
  description: 注文情報を取得する
  parameters:
    - $ref: ../../../params/OrderId.yml
  responses:
    '200':
      description: '正常応答'
      content:
        application/json:
          schema:
            $ref: ../../../responses/orders/OrderResponse.yml
delete:
  tags:
    - orders
  summary: サブスク停止
  description: 契約中のサブスクを停止する
  parameters:
    - $ref: '../../../params/OrderId.yml'
  responses:
    '200':
      description: '正常応答'
      content: null