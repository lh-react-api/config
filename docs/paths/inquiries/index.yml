post:
  tags:
    - inquiries
  summary: お問い合わせ登録
  description: お問い合わせ登録
  requestBody:
    description: リクエスト
    required: true
    content:
      application/json:
        schema:
          $ref: ../../requests/inquiries/CreateRequest.yml
  responses:
    '200':
      description: '正常応答'
      content:
        application/json:
          schema:
            $ref: ../../responses/inquiries/InquiryResponse.yml
