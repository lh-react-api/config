get:
  tags:
    - deliverTimes
  summary: 配送可能時間取得
  description: 配送可能時間取得
  responses:
    '200':
      description: '200'
      content:
        application/json:
          schema:
            $ref: ../../responses/deliverTimes/DeliverTimesResponse.yml