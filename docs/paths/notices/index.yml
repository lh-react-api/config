get:
  tags:
    - notices
  summary: お知らせ一覧
  description: お知らせ一覧 notice_date <= now() && close_date >= now()
  responses:
    '200':
      description: '200'
      content:
        application/json:
          schema:
            $ref: ../../responses/notices/NoticesResponse.yml