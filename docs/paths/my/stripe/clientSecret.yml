get:
  tags:
    - my/stripe
  summary: stripeのclient_secretの発行
  description: カード登録時に利用するclient_secretを発行する
  responses:
    '200':
      description: '正常応答'
      content:
        application/json:
          schema:
            $ref: ../../../responses/my/stripe/StripeClientSecretResponse.yml