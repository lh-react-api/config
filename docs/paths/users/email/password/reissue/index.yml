patch:
  tags:
    - users
  summary: パスワード再設定
  description: パスワードの再設定を行う
  parameters:
    - in: path
      name: email
      description: メールアドレス
      required: true
      schema:
        type: string
        description: メールアドレス
        example: ishikawa@sg.sandbox
  responses:
    '200':
      description: '200'
      content:
        application/json:
          schema:
            type: 'null'
