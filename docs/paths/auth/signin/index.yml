get:
  tags:
    - auth
  summary: ログイン
  description: ログインを行います。csrfのトークンを取得してから実行してください。
  responses:
    '200':
      description: '200'
      content:
        application/json:
          schema:
            $ref: ../../../responses/AuthSignupResponse.yml